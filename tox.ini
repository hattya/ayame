[tox]
minversion = 3.3
envlist = py37, py38, py39, py310, py311
isolated_build = True

[testenv]
deps =
    coverage
    flake8 >= 3.7
    scmver
commands =
# test
    coverage erase
    coverage run --source=ayame setup.py test {posargs:--quiet}
    coverage report
# lint
    flake8

[tox]
requires =
    tox >= 4.25
env_list = py3{10-14}

[testenv]
deps =
    coverage[toml] >= 5.0
    ruff
commands =
# test
    coverage erase
    coverage run --source=ayame -m unittest discover -s tests {posargs}
    coverage report
# lint
    ruff check
